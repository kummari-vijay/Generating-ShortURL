<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>shortid</title>
</head>
<body>
 
    <div class="container my-4 mx-4">
      <div style="margin-left: 400px;">
        <form action="/shortid" method="post">
        <h3>Generate your short urls here</h3>
        <label class="sr-only" for="fullurl">Url</label>
        <input type="url" placeholder="https://www.google.com" name="fullurl"/>
        <button type="submit" class="btn btn-primary">generate</button>

</form>
</div>
<% if(locals.fullurl) {%>
<table class="table table-striped responsive table-sm" style="border : 1px solid black">
    <thead>
    <tr>
    <th>Fullurl</th>
    <th>Shorturl</th>
    <th>Clicks</th>
    </tr>
    </thead>
    <tbody>
        <% fullurl.forEach(shortUrl => { %>
          <tr>
            <td><a href="<%= shortUrl.fullurl %>"><%= shortUrl.fullurl %></a></td>
            <td><a href="<%= shortUrl.shortid %>"><%= shortUrl.shortid %></a></td>
            <td><%=shortUrl.clicks %></td>
          </tr>
        <% }) }%>
      </tbody>
</table>
</div>
</body>
</html>